# Task backlog & Project TODOs — API2UI Studio

## High Priority 🚨
- [x] Add Vitest integration specs matching the **Canonical Scenario** (pasting Device specification, extracting Intent, parameter refinement, and running simulated state upgrades).
- [x] Implement robust frontend error handlers for edge cases where the Gemini API times out or experiences a brief transient connection loss with retry capability.
- [x] Add loading skeletons during state-transition rendering in the compiled execution graph panels.

## Medium Priority ⚡
- [x] Expand the visual diff engine to highlight nested JSON schema changes within API properties (e.g., changing sub-property parameters from `string` to `integer`).
- [x] Integrate OpenAPI YAML text editor with real-time specification validation badges and NCG fingerprinting.
- [x] Build a database view showing compiled micro-apps (jdCards) stored inside the local storage persistence layer.

## Low Priority 💤
- [x] Support custom styling presets (Minimal Light, Tech Dark, Cyberpunk Retro) for compiled jdCard UI projections.
- [x] Support auto-exporting compiled jdCards as ready-to-run React component code bundles (.tsx) or static raw JSON packages.

