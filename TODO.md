# Task backlog & Project TODOs — API2UI Studio

## High Priority 🚨
- [ ] Add Cypress integration specs matching the **Canonical Scenario** (pasting Device specification, extracting Intent, and running simulated state upgrades).
- [ ] Implement robust frontend error handlers for edge cases where the Gemini API times out or experiences a brief transient connection loss.
- [ ] Add loading skeletons during state-transition rendering in the compiled execution graph panels.

## Medium Priority ⚡
- [ ] Expand the visual diff engine to highlight nested JSON schema changes within API properties (e.g., changing sub-property parameters from `string` to `integer`).
- [ ] Integrate auto-completion and error validation badges into the OpenAPI YAML text editor utilizing Monaco Editor properties.
- [ ] Build a database view showing compiled micro-apps (jdCards) stored inside the local storage persistence layer.

## Low Priority 💤
- [ ] Support custom styling presets (e.g., Minimal Light, Tech Dark, Cyberpunk Retro) for compiled jdCard UI projections.
- [ ] Support auto-exporting compiled jdCards as ready-to-run React component code bundles or static raw JSON packages.
